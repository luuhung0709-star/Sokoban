# Editor / IDE
.vs/
.vscode/
.idea/

# Superpowers SDD scratch (ledger, briefs, review packages)
/.superpowers/

# node_modules không bao giờ xuất hiện — web/ không có dependency nào — nhưng
# giữ dòng này để một lần lỡ tay `npm i` không đẩy nghìn file lên repo.
node_modules/
